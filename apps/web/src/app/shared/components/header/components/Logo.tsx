import logo from '../../../../../assets/logo/brick.png';
import '../styles/Logo.scss';

const Logo = () => (
  <div className="logo-container">
    <img src={logo} />
  </div>
);

export default Logo;
